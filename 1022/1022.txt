//알게된 점
//1. 기본적으로 List를 쓰면 동적(가변) 배열로 한다 -> 자바에도 List 존재
// -> 정적(고정) 배열을 하려면 List<타입>.filled(길이, 0); 처럼 해야한다.
//2. Dart에서 랜덤은 Random().nextInt(최대 숫자);와 같이 쓴다.
//+ 랜덤을 사용하려면 import 'dart:math'를 사용해야한다.
//3. Dart에서는 정렬을 배열명.sort(); 와 같이 한다.
//4. 여기선 Boolean을 Bool로 해야한다.